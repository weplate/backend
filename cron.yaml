cron:
- description: 'clear tokens'
  url: /jobs/clear_tokens/
  schedule: every 24 hours
- description: 'send push notifications'
  url: /jobs/send_push/
  schedule: every 15 minutes
- description: 'clear cache'
  url: /jobs/clear_cache/
  schedule: every 168 hours